<h3>this is the view for shop keeper</h3>

<%=link_to "Edit profile" ,edit_user_path(current_user), :method => "get" %><br />

<%=link_to "create shop " ,new_shop_path, :method => "get" %><br />

<%=link_to "sign out" ,destroy_user_session_path, :method => "delete" %>



<center>
	<h2>shops</h2>
		<% if current_user.shops.present? %>
			<% current_user.shops.each do |s|%>
				<%=link_to s.name ,shop_path(s.id), :method => "get" %><br />
				<% end %>
		<% else %>
		
		<% end %>
</center>

